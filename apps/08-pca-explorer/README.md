# PCA Explorer

Load a dataset, choose numeric columns, and inspect the scree plot,
score scatter, and loadings biplot. Component axes are user-selectable.

```r
shiny::runApp("apps/08-pca-explorer")
```
