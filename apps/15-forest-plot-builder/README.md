# Forest Plot Builder

Upload study-level effect sizes with SEs (or use the bundled example),
pick an effect measure and pooling method, and generate a forest plot
with heterogeneity statistics.

```r
shiny::runApp("apps/15-forest-plot-builder")
```

Requires: `metafor`.
