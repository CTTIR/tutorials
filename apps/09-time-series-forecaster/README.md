# Time-Series Forecaster

Fit auto.arima, ETS, naive, seasonal-naive, or STL+ARIMA on a built-in
or uploaded time series; set a hold-out fraction and a forecast horizon;
inspect forecast accuracy and residual diagnostics.

```r
shiny::runApp("apps/09-time-series-forecaster")
```

Requires: `forecast`.
