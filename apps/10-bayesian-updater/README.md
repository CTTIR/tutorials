# Bayesian Updater

Choose a conjugate prior-likelihood pair (beta-binomial, normal-normal
with known variance, gamma-Poisson), specify prior parameters, and watch
the posterior shift as data are entered. Sequential updating available.

```r
shiny::runApp("apps/10-bayesian-updater")
```
