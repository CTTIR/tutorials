# ggplot2 Playground

Pick a dataset, map variables to x/y/colour/facet, choose a geom and
theme, and watch the plot update. The generated ggplot2 code is shown
below so readers can copy it into their own scripts.

```r
shiny::runApp("apps/06-ggplot-playground")
```
