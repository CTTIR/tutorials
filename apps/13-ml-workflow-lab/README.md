# ML Workflow Lab

Build and evaluate a tidymodels workflow on a built-in dataset. Shows
the generated pseudocode alongside CV metrics and a test-set diagnostic
(predicted vs observed or class-probability distribution).

```r
shiny::runApp("apps/13-ml-workflow-lab")
```

Requires: `tidymodels`, `ranger`, `xgboost`, `kknn`.
