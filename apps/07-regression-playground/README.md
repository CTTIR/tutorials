# Regression Playground

Fit linear or generalised linear models from a built-in or uploaded
dataset. Displays coefficient estimates, fit statistics, and diagnostic
plots. Optional two-way interactions and outcome log-transform.

```r
shiny::runApp("apps/07-regression-playground")
```

Requires: `broom`.
