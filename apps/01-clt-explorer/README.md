# CLT Explorer

Interactive visualisation of the central limit theorem: pick a source
distribution, a sample size, and a statistic, and watch the sampling
distribution build up as thousands of replicates are drawn.

Run locally:

```r
shiny::runApp("apps/01-clt-explorer")
```

See `content/shiny/01-clt-explorer.md` for the full specification.
