# Survival Curve Builder

Fit Kaplan-Meier or Cox PH models on built-in or uploaded data. Displays
KM curves with risk tables and optional Cox covariate summaries.

```r
shiny::runApp("apps/11-survival-curve-builder")
```

Requires: `survival`, `survminer`.
