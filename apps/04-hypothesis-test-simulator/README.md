# Hypothesis Test Simulator

Pick a test, an effect size, a sample size, and run thousands of
simulated repetitions. The app shows the null vs alternative sampling
distribution, the p-value histogram, and the empirical rejection rate
(power).

```r
shiny::runApp("apps/04-hypothesis-test-simulator")
```
