# Summary Statistics Lab

Click on a strip plot to place, add, or remove observations; watch mean,
median, SD, MAD, IQR, skewness, and kurtosis update in real time.

```r
shiny::runApp("apps/02-summary-statistics-lab")
```

Demonstrates the differential robustness of mean vs. median and the
influence of single outliers on classical summary statistics.
